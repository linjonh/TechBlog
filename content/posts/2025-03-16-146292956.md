---
arturl_encode: "68747470733a2f2f626c6f672e:6373646e2e6e65742f77656978696e5f37313937303830392f:61727469636c652f64657461696c732f313436323932393536"
layout: post
title: "RabbitMQ实现定时延迟任务"
date: 2025-03-16 11:59:07 +0800
description: "方案选择实现方式（两种）实现缺陷：不一定准时投送，当某一个时间堆积大量消息，那包会有延迟的基于死信队列有这样一个场景，当我是基于消息设置过期时间，第一个投送到空队列的过期时间设置为1h，第二个设置为2m，由于是顺序队列，会导致第二个消息被阻塞，所以基于死信队列还是适合固定时间的延迟那建议还是用插件实现，插件可以实现自定义消息过期时间，利用了一种类似于优先队列的数据结构（具体不了解），虽然依然是监控队首，但是内部消息是根据过期时间长短来决定队首消息的。"
keywords: "RabbitMQ实现定时/延迟任务"
categories: ['八股学习']
tags: ['Rabbitmq', 'Rabbitmq', 'Java']
artid: "146292956"
image:
    path: https://api.vvhan.com/api/bing?rand=sj&artid=146292956
    alt: "RabbitMQ实现定时延迟任务"
render_with_liquid: false
featuredImage: https://bing.ee123.net/img/rand?artid=146292956
featuredImagePreview: https://bing.ee123.net/img/rand?artid=146292956
cover: https://bing.ee123.net/img/rand?artid=146292956
image: https://bing.ee123.net/img/rand?artid=146292956
img: https://bing.ee123.net/img/rand?artid=146292956
---

# RabbitMQ实现定时/延迟任务

方案选择实现方式（两种）

1. 基于死信队列实现
2. 基于插件实现

实现缺陷：不一定准时投送，当某一个时间堆积大量消息，那包会有延迟的

基于死信队列有这样一个场景，当我是基于消息设置过期时间，第一个投送到空队列的过期时间设置为1h，第二个设置为2m，由于是顺序队列，会导致第二个消息被阻塞，所以基于死信队列还是适合固定时间的延迟

那建议还是用插件实现，插件可以实现自定义消息过期时间，利用了一种类似于优先队列的数据结构（具体不了解），虽然依然是监控队首，但是内部消息是根据过期时间长短来决定队首消息的

#### 实现原理

##### RabbitMQ 插件

`rabbitmq_delayed_message_exchange`
插件为每个消息维护独立的延迟计时器。当消息发送到使用该插件的交换器时，插件根据消息
`headers`
中设置的
`x - delay`
值启动计时任务，延迟时间到达后将消息路由到目标队列。它类似于利用一个优先队列来管理消息，按照延迟时间对消息排序，延迟时间短的消息优先处理。

##### 对比RocketMQ

RocketMQ 采用延迟队列和定时任务调度实现定时消息。生产者发送的定时消息先被存储到特殊的延迟队列，这些延迟队列按预设的延迟级别划分，每个级别对应固定延迟时间。RocketMQ 的定时任务周期性检查延迟队列，当消息延迟时间到达，将其从延迟队列取出并发送到目标主题的正常队列。