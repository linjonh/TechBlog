---
layout: post
title: "50条常用的MySQL命令汇总"
date: 2025-09-10T15:07:30+0800
description: "这些命令是MySQL日常使用的核心，熟练掌握将极大提升数据库操作效率。"
keywords: "50条常用的MySQL命令汇总"
categories: ['未分类']
tags: ['数据库', 'Oracle', 'Mysql']
artid: "151404054"
arturl: "https://blog.csdn.net/weixin_44024436/article/details/151404054"
image:
    path: https://api.vvhan.com/api/bing?rand=sj&artid=151404054
    alt: "50条常用的MySQL命令汇总"
render_with_liquid: false
featuredImage: https://bing.ee123.net/img/rand?artid=151404054
featuredImagePreview: https://bing.ee123.net/img/rand?artid=151404054
cover: https://bing.ee123.net/img/rand?artid=151404054
image: https://bing.ee123.net/img/rand?artid=151404054
img: https://bing.ee123.net/img/rand?artid=151404054
---



# 50条常用的MySQL命令汇总

## 50条常用的MySQL命令汇总

---

#### **一、数据库操作**

1. **创建数据库**

   ```sql
   CREATE DATABASE database_name;

   ```
2. **创建数据库并指定字符集**

   ```sql
   CREATE DATABASE database_name CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

   ```
3. **查看所有数据库**

   ```sql
   SHOW DATABASES;

   ```
4. **选择/使用数据库**

   ```sql
   USE database_name;

   ```
5. **查看当前使用的数据库**

   ```sql
   SELECT DATABASE();

   ```
6. **删除数据库**

   ```sql
   DROP DATABASE database_name;

   ```
7. **删除数据库（如果存在）**

   ```sql
   DROP DATABASE IF EXISTS database_name;

   ```
8. **修改数据库字符集**

   ```sql
   ALTER DATABASE database_name CHARACTER SET utf8mb4;

   ```
9. **查看数据库创建语句**

   ```sql
   SHOW CREATE DATABASE database_name;

   ```

---

#### **二、表操作**

10. **创建表**

    ```sql
    CREATE TABLE table_name (
        id INT AUTO_INCREMENT PRIMARY KEY,
        name VARCHAR(50) NOT NULL,
        age INT,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );

    ```
11. **查看所有表**

    ```sql
    SHOW TABLES;

    ```
12. **查看表结构**

    ```sql
    DESCRIBE table_name;
    -- 或
    DESC table_name;
    -- 或
    SHOW COLUMNS FROM table_name;

    ```
13. **查看表的创建语句**

    ```sql
    SHOW CREATE TABLE table_name;

    ```
14. **重命名表**

    ```sql
    RENAME TABLE old_table_name TO new_table_name;

    ```
15. **添加列**

    ```sql
    ALTER TABLE table_name ADD COLUMN column_name VARCHAR(100);

    ```
16. **删除列**

    ```sql
    ALTER TABLE table_name DROP COLUMN column_name;

    ```
17. **修改列定义**

    ```sql
    ALTER TABLE table_name MODIFY COLUMN column_name INT NOT NULL;

    ```
18. **修改列名**

    ```sql
    ALTER TABLE table_name CHANGE old_column_name new_column_name VARCHAR(100);

    ```
19. **添加主键**

    ```sql
    ALTER TABLE table_name ADD PRIMARY KEY (id);

    ```
20. **删除主键**

    ```sql
    ALTER TABLE table_name DROP PRIMARY KEY;

    ```
21. **添加唯一索引**

    ```sql
    ALTER TABLE table_name ADD UNIQUE (column_name);

    ```
22. **添加普通索引**

    ```sql
    ALTER TABLE table_name ADD INDEX idx_name (column_name);

    ```
23. **添加复合索引**

    ```sql
    ALTER TABLE table_name ADD INDEX idx_composite (col1, col2);

    ```
24. **删除索引**

    ```sql
    DROP INDEX index_name ON table_name;

    ```
25. **删除表**

    ```sql
    DROP TABLE table_name;

    ```
26. **清空表（删除所有数据）**

    ```sql
    TRUNCATE TABLE table_name;

    ```
27. **查看表索引**

    ```sql
    SHOW INDEX FROM table_name;

    ```

---

#### **三、数据操作**

28. **插入单条数据**

    ```sql
    INSERT INTO table_name (col1, col2) VALUES ('value1', 'value2');

    ```
29. **插入多条数据**

    ```sql
    INSERT INTO table_name (col1, col2) VALUES 
    ('v1', 'v2'), 
    ('v3', 'v4');

    ```
30. **插入数据（忽略重复）**

    ```sql
    INSERT IGNORE INTO table_name (col1, col2) VALUES ('value1', 'value2');

    ```
31. **插入或更新（存在则更新）**

    ```sql
    INSERT INTO table_name (id, name) VALUES (1, 'Alice') ON DUPLICATE KEY UPDATE name='Alice';

    ```
32. **更新数据**

    ```sql
    UPDATE table_name SET column_name = 'new_value' WHERE id = 1;

    ```
33. **更新多列**

    ```sql
    UPDATE table_name SET col1 = 'v1', col2 = 'v2' WHERE condition;

    ```
34. **删除数据**

    ```sql
    DELETE FROM table_name WHERE id = 1;

    ```
35. **删除所有数据（不重置自增）**

    ```sql
    DELETE FROM table_name;

    ```
36. **查询所有数据**

    ```sql
    SELECT * FROM table_name;

    ```
37. **查询指定列**

    ```sql
    SELECT col1, col2 FROM table_name;

    ```
38. **带条件查询**

    ```sql
    SELECT * FROM table_name WHERE age > 18;

    ```
39. **模糊查询**

    ```sql
    SELECT * FROM table_name WHERE name LIKE '%张%';

    ```
40. **范围查询**

    ```sql
    SELECT * FROM table_name WHERE age BETWEEN 18 AND 30;

    ```
41. **IN 查询**

    ```sql
    SELECT * FROM table_name WHERE id IN (1, 2, 3);

    ```
42. **排序查询**

    ```sql
    SELECT * FROM table_name ORDER BY age DESC;

    ```
43. **分页查询**

    ```sql
    SELECT * FROM table_name LIMIT 10 OFFSET 0;
    -- 或
    SELECT * FROM table_name LIMIT 0, 10;

    ```
44. **去重查询**

    ```sql
    SELECT DISTINCT column_name FROM table_name;

    ```
45. **聚合函数查询**

    ```sql
    SELECT COUNT(*), AVG(age), MAX(age), MIN(age), SUM(age) FROM table_name;

    ```
46. **分组查询**

    ```sql
    SELECT department, COUNT(*) FROM employees GROUP BY department;

    ```
47. **分组后筛选（HAVING）**

    ```sql
    SELECT department, COUNT(*) FROM employees GROUP BY department HAVING COUNT(*) > 5;

    ```
48. **多表连接查询（INNER JOIN）**

    ```sql
    SELECT u.name, o.order_id FROM users u INNER JOIN orders o ON u.id = o.user_id;

    ```
49. **左连接查询（LEFT JOIN）**

    ```sql
    SELECT u.name, o.order_id FROM users u LEFT JOIN orders o ON u.id = o.user_id;

    ```
50. **子查询**

    ```sql
    SELECT * FROM users WHERE id IN (SELECT user_id FROM orders WHERE amount > 100);

    ```

---

#### **补充说明**

* **安全操作**：执行 `DROP`, `DELETE`, `UPDATE` 等操作前，建议先备份数据或在测试环境验证。
* **性能优化**：合理使用索引、避免 `SELECT *`、优化 `WHERE` 条件可提升查询效率。
* **字符集**：推荐使用 `utf8mb4` 字符集以支持完整的 Unicode（如 emoji）。
* **事务**：对于需要保证数据一致性的操作，使用 `BEGIN`, `COMMIT`, `ROLLBACK` 控制事务。

这些命令是MySQL日常使用的核心，熟练掌握将极大提升数据库操作效率。



