---
layout: post
title: "niceLeetCode-每日一题-1717.-删除子字符串的最大得分-贪心"
date: 2025-07-23T13:55:48+0800
description: "【代码】(nice!!!)(LeetCode 每日一题) 1717. 删除子字符串的最大得分 (贪心)"
keywords: "(nice!!!)(LeetCode 每日一题) 1717. 删除子字符串的最大得分 (贪心)"
categories: ['Leetcode', 'C']
tags: ['职场和发展', '算法', 'Leetcode', 'Java', 'Go', 'C']
artid: "149569276"
arturl: "https://blog.csdn.net/weixin_46028214/article/details/149569276"
image:
    path: https://api.vvhan.com/api/bing?rand=sj&artid=149569276
    alt: "niceLeetCode-每日一题-1717.-删除子字符串的最大得分-贪心"
render_with_liquid: false
featuredImage: https://bing.ee123.net/img/rand?artid=149569276
featuredImagePreview: https://bing.ee123.net/img/rand?artid=149569276
cover: https://bing.ee123.net/img/rand?artid=149569276
image: https://bing.ee123.net/img/rand?artid=149569276
img: https://bing.ee123.net/img/rand?artid=149569276
---



# (nice!!!)(LeetCode 每日一题) 1717. 删除子字符串的最大得分 (贪心)



### 题目：[1717. 删除子字符串的最大得分](https://leetcode.cn/problems/maximum-score-from-removing-substrings/?envType=daily-question&envId=2025-07-23)

![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/4f462dd7c34345a48e94d503cc510de0.png)

**思路：贪心，时间复杂度0(n)。**

**非a/b的字符，都可视为分隔符，将字符串s分割为多个只有ab的子串。然后在子串里，优先满足ab的情况(假设ab的分＞ba的分)，细节看注释。**

**C++版本：**

```cpp
class Solution {
public:
    int maximumGain(string s, int x, int y) {
    	// 优先处理分数大的情况，
        char a='a',b='b';
        if(x<y){
            swap(a,b);
            swap(x,y);
        }
        // sum是答案
        // cta是当前子串里a字符的数量，ctb是当前子串里b字符的数量
        int sum=0,cta=0,ctb=0;
        for(auto c:s){
        	// 优先判断ab的情况，所以遇到a字符时，cta++
            if(c==a){
                cta++;
            }else if(c==b){
            	//遇到b字符时，判断前面是否有a
                if(cta>0){
                    cta--;
                    sum+=x;
                }else{
                	//无a就记录b字符的数量，用于后面ba的情况
                    ctb++;
                }
            }else{
            	//遇到非a、b的字符，那么这一段子串处理就结束了
            	// ab的情况，前面都已经处理了，这里再处理ba的情况
                sum+=y*min(cta,ctb);
                cta=0;
                ctb=0;
            }
        }
        // 处理边界情况
        sum+=y*min(cta,ctb);
        return sum;
    }
};

```

**JAVA版本：**

```java
class Solution {
    public int maximumGain(String s, int x, int y) {
        char a='a',b='b';
        if(x<y){
            char t=a;
            a=b;
            b=t;
            int c=x;
            x=y;
            y=c;
        }
        int sum=0,cta=0,ctb=0;
        for(var c:s.toCharArray()){
            if(c==a){
                cta++;
            }else if(c==b){
                if(cta>0){
                    cta--;
                    sum+=x;
                }else{
                    ctb++;
                }
            }else{
                sum+=Math.min(cta,ctb)*y;
                cta=0;
                ctb=0;
            }
        }
        sum+=Math.min(cta,ctb)*y;
        return sum;
    }
}

```

**GO版本：**

```go
func maximumGain(s string, x int, y int) int {
    a,b:='a','b'
    if x<y {
        x,y=y,x
        a,b=b,a
    }
    sum,cta,ctb:=0,0,0
    for _,c:=range s {
        if c==a {
            cta++
        }else if c==b {
            if cta>0 {
                cta--
                sum+=x
            }else {
                ctb++
            }
        }else{
            sum+=min(cta,ctb)*y
            cta=0
            ctb=0
        }
    }
    sum+=min(cta,ctb)*y
    return sum
}

```



