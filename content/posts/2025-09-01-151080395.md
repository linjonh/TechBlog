---
layout: post
title: "9.1C类中特殊的成员函数"
date: 2025-09-01T20:06:31+0800
description: "设计一个Per类，类中包含私有成员:姓名、年龄、指针成员身高、体重，再设计一个Stu类，类中包含私有成员:成绩、Per类对象p1，设计这两个类的构造函数、析构函数和拷贝构造函数。"
keywords: "9.1C++——类中特殊的成员函数"
categories: ['未分类']
tags: ['开发语言', '学习', 'C']
artid: "151080395"
arturl: "https://blog.csdn.net/2303_78618914/article/details/151080395"
image:
    path: https://api.vvhan.com/api/bing?rand=sj&artid=151080395
    alt: "9.1C类中特殊的成员函数"
render_with_liquid: false
featuredImage: https://bing.ee123.net/img/rand?artid=151080395
featuredImagePreview: https://bing.ee123.net/img/rand?artid=151080395
cover: https://bing.ee123.net/img/rand?artid=151080395
image: https://bing.ee123.net/img/rand?artid=151080395
img: https://bing.ee123.net/img/rand?artid=151080395
---



# 9.1C++——类中特殊的成员函数

## 一、思维导图

![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/83f4d8ef88f34364a86b628b6a46f508.png#pic_center)

## 二、编程练习

设计一个Per类，类中包含私有成员:姓名、年龄、指针成员身高、体重，再设计一个Stu类，类中包含私有成员:成绩、Per类对象p1，设计这两个类的构造函数、析构函数和拷贝构造函数。

### 1、代码

```cpp
#include <iostream>

using namespace std;
class Per
{
private:
    string name; //姓名
    int age;  //年龄
    int *height;  //身高
    int *weight;  //体重
public:
    //构造函数
    Per(string name="--",int age=0,int height=0,int weight=0):name(name),age(age),height(new int(height)),weight(new int(weight))
    {
        cout << "Per::构造函数" << endl;
    }
    //拷贝构造函数
    Per(const Per &other):name(other.name),age(other.age),height(new int(*(other.height))),weight(new int(*(other.weight)))
    {
        cout << "Per::拷贝构造函数" << endl;
    }
    //析构函数
    ~Per()
    {
        delete height;
        delete weight;
        height=nullptr;
        weight=nullptr;
        cout << "Per::析构函数" << endl;

    }
};
class Stu
{
private:
    double score; //成绩
    Per p1;
public:
    //构造函数
    Stu(double score=0,string name="--",int age=0,int height=0,int weight=0):score(score),p1(name,age,height,weight)
    {
        cout << "Stu::构造函数" << endl;
    }
    //拷贝构造函数
    Stu(const Stu &other):score(other.score),p1(other.p1)
    {
        cout << "Stu::拷贝构造函数" << endl;
    }
    //析构函数
    ~Stu()
    {
        cout << "Stu::析构函数" << endl;

    }
};
int main()
{
    Stu s1(88,"lihua",18,175,55);
    Stu S2(s1);
    return 0;
}


```

### 2、现象

![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/bc6d50ada460487d86d0d69e47b27bb5.png#pic_center)



